{
  "build": {
    "builder": "NIXPACKS",
    "rootDir": "backend",
    "nixpacksPlan": {
      "providers": ["node"],
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs", "npm"]
        },
        "build": {
          "cmd": "npm install"
        }
      }
    }
  },
  "deploy": {
    "startCommand": "npm start"
  }
}