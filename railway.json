{
  "build": {
    "builder": "NIXPACKS",
    "rootDir": "backend",
    "nixpacksPlan": {
      "providers": ["node"],
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs"]
        },
        "build": {
          "cmd": "npm install"
        }
      }
    }
  },
  "deploy": {
    "startCommand": "npm start"
  }
}